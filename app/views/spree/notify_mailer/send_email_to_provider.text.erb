Estimada Empresa GANE,

Le remito los datos del pedido de neumaticos con destino a la direccion abajo reseñada.

============================================================
Datos del Pedido
============================================================
<% @order.line_items.each do |item| %>
  <%= item.variant.sku %> <%= raw(item.variant.product.name) %> <%= raw(item.variant.options_text) -%> (<%=item.quantity%>) @ <%= number_to_currency item.price %> = <%= number_to_currency(item.price * item.quantity) %>
<% end %>
============================================================
Subtotal: <%= number_to_currency @order.item_total %>
<% @order.adjustments.eligible.each do |adjustment| %>
  <%= raw(adjustment.label) %> <%= number_to_currency(adjustment.amount) %>
<% end %>
Pedido Total: <%= number_to_currency(@order.total) %>

============================================================
Datos del Cliente
============================================================

Nombre: <%= @order.ship_address.firstname.capitalize %>
Apellidos: <%= @order.ship_address.lastname.capitalize %>
Direccion: <%= @order.ship_address.address1.capitalize + @order.ship_address.address2.capitalize %>
Poblacion: <%= @order.ship_address.city.capitalize %>
Codigo Postal: <%= @order.ship_address.zipcode %>
Provincia: <%= @order.ship_address..state.name.capitalize %>
Teléfono: <%= @order.ship_address.phone %>  
