Estimada Empresa <%= @line_items.first.variant.product.supplier.title %>,

Le remito los datos del pedido de neumaticos con destino a la direccion abajo reseñada.

============================================================
Datos del Pedido
============================================================
<% total = 0 -%>
<% @line_items.each do |item| %>
  <%= item.variant.sku %> <%= raw(item.variant.product.name) %> <%= raw(item.variant.tire_width.name) unless item.variant.tire_width_id.nil? -%>/<%= raw(item.variant.tire_serial.name) unless item.variant.tire_serial_id.nil? -%>/<%= raw(item.variant.tire_innertube.name) unless item.variant.tire_innertube_id.nil? -%>/<%= raw(item.variant.tire_speed_code.name) unless item.variant.tire_speed_code_id.nil? -%> (<%=item.quantity%>)  <%= number_to_currency item.variant.cost_price %> = <%= number_to_currency(item.variant.cost_price * item.quantity) %> <% total += item.variant.cost_price * item.quantity %>
<% end %>
============================================================
Subtotal: <%= number_to_currency total %>
<% @order.adjustments.eligible.each do |adjustment| %>
  <%= raw(adjustment.label) %> <%= number_to_currency(adjustment.amount) %>
<% end %>
Pedido Total: <%= number_to_currency total %>

============================================================
Datos del Cliente
============================================================

Nombre: <%= @order.bill_address.firstname %>
Apellidos: <%= @order.bill_address.lastname %>
Direccion: <%= @order.bill_address.address1 %>
Poblacion: <%= @order.bill_address.city %>
Codigo Postal: <%= @order.bill_address.zipcode %>
Provincia: <%= @order.bill_address.state.name %>
Teléfono: <%= @order.bill_address.phone %>  
